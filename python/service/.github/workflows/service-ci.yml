name: service-ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_call:
    secrets:
      CODECOV_TOKEN:
        required: false

jobs:
  test:
    name: Test & Coverage
    uses: Neosofia/platform-workflows/.github/workflows/_test-python.yml@12c771d8c53ee656ad0b3fd276887916b056be9a
    with:
      upload_coverage: true
      codecov_flags: service-template
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  scan:
    name: Vulnerability Scan
    uses: Neosofia/platform-workflows/.github/workflows/_scan.yml@12c771d8c53ee656ad0b3fd276887916b056be9a
